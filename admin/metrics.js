window.LDP_METRICS=Object.freeze({
  calculate(input={}){const n=value=>Math.max(0,Number(value)||0),spend=n(input.spend),impressions=n(input.impressions),clicks=n(input.clicks),leads=n(input.leads),sales=n(input.sales),revenue=n(input.revenue),ratio=(a,b,m=1)=>b?a/b*m:null;return{ctr:ratio(clicks,impressions,100),cpc:ratio(spend,clicks),cpl:ratio(spend,leads),conversion:ratio(sales,leads,100),costPerSale:ratio(spend,sales),roas:ratio(revenue,spend)}},
  evaluate(current={},previous={}){const now=this.calculate(current),before=this.calculate(previous),alerts=[];if(now.cpl!=null&&before.cpl!=null&&now.cpl>before.cpl*1.25)alerts.push({code:'COST_SPIKE',severity:'warning',message:'CPL aumentou mais de 25%.'});if(Number(current.impressions)>=1000&&Number(current.clicks)===0)alerts.push({code:'NO_RESULT',severity:'critical',message:'Campanha com impressões e sem cliques.'});if(now.ctr!=null&&before.ctr!=null&&now.ctr<before.ctr*.7)alerts.push({code:'CREATIVE_FATIGUE',severity:'warning',message:'CTR caiu mais de 30%; revisar o criativo.'});if(Number(current.unattendedLeads)>0)alerts.push({code:'UNATTENDED_LEADS',severity:'critical',message:`${Number(current.unattendedLeads)} lead(s) aguardando atendimento.`});return{metrics:now,alerts}},
  winner(campaigns=[]){return campaigns.map(c=>({...c,metrics:this.calculate(c)})).filter(c=>c.metrics.cpl!=null).sort((a,b)=>a.metrics.cpl-b.metrics.cpl||(b.metrics.roas||0)-(a.metrics.roas||0))[0]||null}
});
addEventListener('DOMContentLoaded',()=>{const button=document.querySelector('[data-tab="intelligence"]'),heading=document.querySelector('[data-panel="intelligence"] h2'),badge=document.querySelector('.ai-summary span');if(button){button.textContent='Análises operacionais';button.addEventListener('click',()=>setTimeout(()=>{const title=document.querySelector('#page-title'),crumb=document.querySelector('#breadcrumb-current');if(title)title.textContent='Análises operacionais';if(crumb)crumb.textContent='Análises operacionais'}))}if(heading)heading.textContent='Análises operacionais';if(badge)badge.textContent='Regras'});
